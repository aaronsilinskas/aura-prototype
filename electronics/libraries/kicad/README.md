# KiCad libraries

Shared KiCad symbol libraries, footprint libraries, and 3D models referenced by
more than one KiCad-drawn PCB under `../../pcbs/`.

Empty for now — this scaffold carries no shared parts yet. See `../README.md`
for when a part belongs here versus staying local to a single PCB.
